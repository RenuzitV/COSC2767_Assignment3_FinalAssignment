apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: db-service
    app: demo-store-app
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
     name: mariadb-pod
     app: demo-store-app